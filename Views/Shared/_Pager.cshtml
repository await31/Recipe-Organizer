@model BusinessObjects.Models.Pager

@{
    var currentController = ViewContext.RouteData.Values["Controller"] as string;
    var currentAction = ViewContext.RouteData.Values["Action"] as string;
}

<style>
    .pagination .active a {
        background-color: black !important;
        color: white !important;
    }
    
    .pagination a {
        background-color: white !important;
        color: black !important;
    }
</style>

@*
@if (Model.TotalPages > 0) {
    <ul class="pagination justify-content-end">
        @if (Model.CurrentPage > 1) {
            <li class="page-item">
                <a class="page-link" asp-controller="@currentController" asp-action="@currentAction" asp-route-pg="1">First</a>
            </li>
            <li class="page-item">
                <a class="page-link" asp-controller="@currentController" asp-action="@currentAction" asp-route-pg="@(Model.CurrentPage-1)">Previous</a>
            </li>
        }
        @for (var pge = Model.StartPage; pge <= Model.EndPage; pge++) {
            <li class="page-item @(pge == Model.CurrentPage ? "active" : "")">
                <a class="page-link" asp-controller="@currentController" asp-action="@currentAction" asp-route-pg="@pge">@pge</a>
            </li>
        }
        @if (Model.CurrentPage < Model.TotalPages) {
            <li class="page-item">
                <a class="page-link" asp-controller="@currentController" asp-action="@currentAction" asp-route-pg="@(Model.CurrentPage+1)">Next</a>
            </li>
            <li class="page-item">
                <a class="page-link" asp-controller="@currentController" asp-action="@currentAction" asp-route-pg="@(Model.TotalPages)">Last</a>
            </li>
        }
    </ul>
}
*@


       @if (Model.TotalPages > 0)
     {
         <ul class="pagination justify-content-end">
           @if (Model.CurrentPage > 1)
             {
            <li class="page-item">
                <a class="page-link" asp-controller="@currentController" asp-action="@currentAction" asp-route-pg="1"
                asp-route-PrepTime="@Model.PrepTime"
                asp-route-IncludeList="@Model.IncludeList"
                asp-route-ExcludeList="@Model.ExcludeList"
                asp-route-SortBy="@Model.SortBy"
                asp-route-RecipeCategory="@Model.RecipeCategory"
                asp-route-Difficulty="@Model.Difficulty">
                   First</a>
            </li>
            <li class="page-item">
                <a class="page-link" asp-controller="@currentController" asp-action="@currentAction" asp-route-pg="@(Model.CurrentPage - 1)"
                   asp-route-PrepTime="@Model.PrepTime"
                asp-route-IncludeList="@Model.IncludeList"
                asp-route-ExcludeList="@Model.ExcludeList"
                asp-route-SortBy="@Model.SortBy"
                asp-route-RecipeCategory="@Model.RecipeCategory"
                asp-route-Difficulty="@Model.Difficulty">
                   Previous</a>
            </li>
        }
        @for (var pge = Model.StartPage; pge <= Model.EndPage; pge++)
        {
            <li class="page-item @(pge == Model.CurrentPage ? "active" : "")">
                <a class="page-link" asp-controller="@currentController" asp-action="@currentAction" asp-route-pg="@pge"
                  asp-route-PrepTime="@Model.PrepTime"
                asp-route-IncludeList="@Model.IncludeList"
                asp-route-ExcludeList="@Model.ExcludeList"
                asp-route-SortBy="@Model.SortBy"
                asp-route-RecipeCategory="@Model.RecipeCategory"
                asp-route-Difficulty="@Model.Difficulty">
                   @pge</a>
            </li>
        }
        @if (Model.CurrentPage < Model.TotalPages)
        {
            <li class="page-item">
                <a class="page-link" asp-controller="@currentController" asp-action="@currentAction" asp-route-pg="@(Model.CurrentPage + 1)"
                   asp-route-PrepTime="@Model.PrepTime"
                asp-route-IncludeList="@Model.IncludeList"
                asp-route-ExcludeList="@Model.ExcludeList"
                asp-route-SortBy="@Model.SortBy"
                asp-route-RecipeCategory="@Model.RecipeCategory"
                asp-route-Difficulty="@Model.Difficulty">
                   Next</a>
            </li>
            <li class="page-item">
                <a class="page-link" asp-controller="@currentController" asp-action="@currentAction" asp-route-pg="@Model.TotalPages"
                   asp-route-PrepTime="@Model.PrepTime"
                asp-route-IncludeList="@Model.IncludeList"
                asp-route-ExcludeList="@Model.ExcludeList"
                asp-route-SortBy="@Model.SortBy"
                asp-route-RecipeCategory="@Model.RecipeCategory"
                asp-route-Difficulty="@Model.Difficulty">
                   Last</a>
            </li>
       }
         </ul>
}